apiVersion: v1
kind: PersistentVolume
metadata:
  name: userstore
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/local/userstore/"
